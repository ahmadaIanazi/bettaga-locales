import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function HeaderModal({ title, right, left, rightIcon, leftIcon }) {
  return (
    <View>
      <Text>HeaderModal</Text>
    </View>
  )
}

const styles = StyleSheet.create({})